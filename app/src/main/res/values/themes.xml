<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="Base.Theme.NoteKeeper" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NoteKeeper" parent="Base.Theme.NoteKeeper" />

    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_app</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/notekeeper</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.NoteKeeper</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
    </style>
</resources>